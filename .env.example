# 一个 Vercel AI Gateway Key 即可同时调用 Jev 和 GPT。
AI_GATEWAY_API_KEY=replace_me

# 以下均为可选配置；不修改则使用默认值。
BTC_SYMBOL=BTCUSDT
BINANCE_BASE_URL=https://data-api.binance.vision
JEV_MODEL=typesafe-ai/jev
GPT_MODEL=openai/gpt-5.6-sol
